/** Default list page size aligned with docs/dev/api.md */
export const DEFAULT_LIST_PAGE_SIZE = 20

/** Server-enforced max pageSize; do not use as a silent “load everything” size. */
export const MAX_LIST_PAGE_SIZE = 100
