## Why

母 change P7：清单内自定义组；工具栏已有「清单分组」占位。

## What Changes

- 清单上下文 + `LIST_GROUP`（及无字段分组时）：默认组、新建/删组、分区与拖拽
- `USE_LOCAL_LIST_GROUPS` 直至 `-api`
- 起草 `openspec/lanes/workspace-task-list-groups/contract.md`
- **不改** Java

## Capabilities

### Modified Capabilities

- `task`：清单内组 UI（前端）

## Impact

`web/`、contract、看板
