## 1. 去临时与联调

- [x] 1.1 删除 `quickViewsLocal.ts`；store 仅走 `scope=ALL` / `ASSIGNED_BY_ME`
- [x] 1.2 「已完成」改用 `scope=ALL` + `status=DONE`（可见并集）
- [x] 1.3 更新空状态文案；contract / 看板 → done；母 change §2.3
- [x] 1.4 `pnpm build` + `pnpm typecheck` 通过
