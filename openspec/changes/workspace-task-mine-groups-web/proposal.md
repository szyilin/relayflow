## Why

母 change P5：工具栏已有「自定义分组」占位；需前端先行默认组 + 自建组，按个人逻辑分区列表/看板。

## What Changes

- 「我负责的」+ `groupBy=PERSONAL_CUSTOM`：默认组、新建组、删组回默认、跨组拖拽
- `USE_LOCAL_MINE_GROUPS` 直至 `-api` / integrate
- 起草 `openspec/lanes/workspace-task-mine-groups/contract.md`
- **不改** Java

## Capabilities

### Modified Capabilities

- `task`：我负责的个人自定义分组 UI（前端）

## Impact

`web/`、contract、看板
