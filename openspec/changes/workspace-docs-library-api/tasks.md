# Tasks：workspace-docs-library-api（[后端]）

- [x] 1. ErrorCodeConstants（DOC_*）+ contract 回写数字码
- [x] 2. Library tree GET + node POST/PUT/DELETE（防环、级联软删）
- [x] 3. Document GET + body PUT（乐观锁）+ recent GET
- [x] 4. Export MD（TipTap → Markdown）；docx/pdf → DOC_EXPORT_FORMAT_UNSUPPORTED
- [x] 5. `./mvnw -pl relayflow-server -am compile`；curl 脚本（可选）
- [x] 6. 母 change §3 勾选；看板 api → ready
