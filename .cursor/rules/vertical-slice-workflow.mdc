---
description: 业务功能按纵向切片交付 — 后端 API 与 web/ 同批对接，界面可验证
alwaysApply: true
---

# 纵向切片工作流（业务功能必读）

脚手架 change（`scaffold-*`）除外，**用户可感知功能**必须按 **纵向切片** 实现。

## 必须

1. 每个切片：**后端 API + 前端对接**（有 UI 时）须可浏览器验证；**推荐**拆为 `{slice}-api` + `{slice}-web` + `{slice}-integrate` 并行（见 [`parallel-lane-workflow.md`](../../docs/dev/parallel-lane-workflow.md)）
2. 单 change 模式：tasks 按后端 / 前端 / 联调 分组，结构见 [`vertical-slice-workflow.md`](../../docs/dev/vertical-slice-workflow.md)
3. 完成前验证：`mvn compile`；含 `web/` 时 **`pnpm build`**；有 UI 时写明浏览器路径（如 `/admin/login`）
4. 已有后端无前端的能力（如登录 API）→ 优先开切片 **补前端**，勿再堆纯后端 change

## 禁止

- 连续多会话只写后端、长期无对应 `web/` 页面（tasks 标 `[平台]` 的除外）
- 单次会话实现整个 V1 / 全部模块
- 前端页面直接 `fetch` 裸 URL，须走 `web/src/api/admin|app/`

## 下一优先（管理端）

管理端须先 **UI 定调**（见 [`docs/dev/admin-ui-workflow.md`](../../docs/dev/admin-ui-workflow.md)），再纵向切片接 API：

1. `admin-ui-prototype` — Mock 全壳层 + 人工签字
2. `admin-ui-design-direction` — 从定稿原型抽取规则
3. `admin-login-slice` — 真 JWT（保留原型 UI）
4. `admin-shell-web` + `admin-shell-integrate` — 租户名、退出（`-api` 已归档；看板 [`api-integration-board.md`](../../docs/dev/api-integration-board.md)）
5. `[平台]` tenant §5 Redis/MinIO/WS — 无 UI，可独立 change

全文：[`docs/dev/vertical-slice-workflow.md`](../../docs/dev/vertical-slice-workflow.md)
