---
description: 业务功能按纵向切片 · 前端优先交付 — web UI 先行，API 后行
alwaysApply: true
---

# 纵向切片工作流（业务功能必读）

脚手架 change（`scaffold-*`）除外，**用户可感知功能**必须按 **纵向切片 · 前端优先** 实现。

## 必须

1. 默认顺序：**`-web`（UI+Mock+contract 草案）→ `-api` → `-integrate`**（见 [`frontend-first-workflow.md`](../../docs/dev/frontend-first-workflow.md)）
2. **唯一登录页** `/app/login`；登录后进工作台；管理端 `/admin` 同一 JWT
3. 完成前：`-web` 须 **`pnpm build`** + 浏览器路径；integrate 须联调
4. 已有后端无 UI → 开 **`-web`** 补 UI，勿再堆纯 `-api`

## 禁止

- **先开 `-api` 无对应 `-web` UI**（`[平台]` 除外）
- 连续多会话只写后端
- 页面直接 `import mocks/`（仅 store 内）

## 下一优先

1. `admin-shell-web` + integrate — 壳层租户名
2. `admin-user-list-web` — **用户列表 UI 先行**，再 `-api`

全文：[`frontend-first-workflow.md`](../../docs/dev/frontend-first-workflow.md) · [`vertical-slice-workflow.md`](../../docs/dev/vertical-slice-workflow.md)
