---
description: 员工工作台 UI（/app/*）页面与 token 约定
globs: web/src/pages/app/**,web/src/components/workspace/**,web/src/assets/css/workspace.css,web/src/composables/useWorkspace*.ts
alwaysApply: false
---

# 员工工作台 UI

编辑 `/app/*` 或 `components/workspace/` 时遵循：

1. [`docs/dev/workspace-ui-patterns.md`](../../docs/dev/workspace-ui-patterns.md) — 壳层、数据层
2. [`docs/dev/workspace-ui-tokens.md`](../../docs/dev/workspace-ui-tokens.md) — `--ws-*` token

## 应当

- 使用 `WorkspaceShell` + `workspace.css`；**禁止**混用管理端 `UDashboardSidebar` / `--admin-*`
- 页面 → **Store** → `api/`；`-web` 临时假数据只放 store，integrate 删除
- HTTP 走 `web/src/api/request.ts`（axios）

## 禁止

- 页面直接 `import mocks/`；常驻 `web/src/mocks/`；已废弃的 `isApiUnavailable` 全局回退
- 引入 Element Plus 等第二套 UI 库
