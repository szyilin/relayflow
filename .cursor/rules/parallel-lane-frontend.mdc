---
description: 并行 Lane — 前端 OpenSpec（*-web change）**第一步**；UI 先行 + contract 草案
globs: web/**/*
alwaysApply: false
---

# 并行 Lane · 前端（`*-web`）— 第一步

当用户指派 **前端 lane** 或 change 名以 **`-web`** 结尾时，遵循本规则。

## 必读

1. [`docs/dev/frontend-first-workflow.md`](../../docs/dev/frontend-first-workflow.md) — **默认 `-web` 先于 `-api`**
2. [`docs/dev/api-integration-board.md`](../../docs/dev/api-integration-board.md)
3. `openspec/changes/<slice>-web/tasks.md`
4. [`docs/dev/admin-ui-patterns.md`](../../docs/dev/admin-ui-patterns.md) / [`workspace-ui-patterns.md`](../../docs/dev/workspace-ui-patterns.md)

## 应当

- 只编辑 **`web/`** 与当前 **`*-web`** change 文档
- 实现 **页面 + Store**；API 未就绪时 store 内可用临时本地数据撑 UI（**integrate 必须删除**）；勿再实现已废弃的 `isApiUnavailable` / 常驻 `mocks/`
- **起草** `openspec/lanes/{slice}/contract.md`（路径、字段、鉴权、curl）
- 更新看板 **`web → in_progress / ui_ready`**
- 完成前 **`cd web && pnpm build && pnpm typecheck`**
- API 调用走 `web/src/api/admin/` 或 `app/`；页面 **不得** import `mocks/`

## 禁止

- 在 UI 未 `pnpm build` + `pnpm typecheck` 通过前 archive `-web`
- 未起草 contract 就要求 `-api` 实现（除非 tasks 写明沿用已有 API）
- 修改 Java（`vite.config` proxy、`VITE_*` 除外）

## 会话开场白（建议）

> Using change: `<slice>-web`（前端 lane · 第一步）。先做 UI + contract 草案；临时数据仅放 store。
