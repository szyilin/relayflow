---
description: 并行 Lane — 后端 OpenSpec（*-api change）**第二步**；按 contract 实现 API
globs: relayflow-*/**/*
alwaysApply: false
---

# 并行 Lane · 后端（`*-api`）— 第二步

当用户指派 **后端 lane** 或 change 名以 **`-api`** 结尾时，遵循本规则。

## 必读

1. [`docs/dev/frontend-first-workflow.md`](../../docs/dev/frontend-first-workflow.md) — **`-web` 应先于 `-api`**
2. [`docs/dev/parallel-lane-workflow.md`](../../docs/dev/parallel-lane-workflow.md)
3. [`docs/dev/api-integration-board.md`](../../docs/dev/api-integration-board.md)
4. `openspec/changes/<slice>-api/tasks.md`
5. **`openspec/lanes/<slice>/contract.md`** — 通常由 **`-web` 起草**；本 lane **按 contract 实现**
6. [`docs/dev/api.md`](../../docs/dev/api.md)、[`docs/dev/database.md`](../../docs/dev/database.md)

## 应当

- 只编辑 **Java**、Flyway、`deploy/`（tasks 要求时）
- **前置**：`-web` 已 ui_ready 或 contract 已冻结（单人顺序：web 完成后才开 api）
- 实现字段 **与 contract / 前端页面一致**；若需改字段，先改 contract 并通知 integrate
- `mvn compile` + curl；看板 `api → archived`；archive `-api`

## 禁止

- **在无 `-web` contract 草案时擅自定 API 形状**（`[平台]` 除外）
- 改 `web/`（CORS/Security 白名单除外）
- 勾选 `-web` / `-integrate` tasks

## 会话开场白（建议）

> Using change: `<slice>-api`（后端 lane · 第二步）。读 `-web` 起草的 contract.md 并实现。
